import {ArrowRight,MapPin} from 'lucide-react';import {Link} from 'react-router-dom';import type {DemoJob} from '../../data/types';
export function JobCard({job}:{job:DemoJob}){return <Link to={`/jobs/${job.id}`} className="glass-panel block rounded-3xl p-4 transition hover:-translate-y-1 hover:bg-white/10"><div className="flex items-start gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-gradient-to-br from-cyanstar to-limestar font-black text-slate-950">{job.logo}</div><div className="min-w-0 flex-1"><h3 className="truncate text-base font-bold">{job.title}</h3><p className="text-sm text-soft">{job.company}</p><p className="mt-2 flex items-center gap-1 text-xs text-soft"><MapPin size={14}/>{job.location} · {job.type}</p></div><div className="text-right"><p className="text-lg font-black text-limestar">{job.match}%</p><p className="text-[10px] uppercase text-soft">match</p></div></div><div className="mt-4 flex flex-wrap gap-2">{job.skills.map(skill=><span key={skill} className="rounded-full bg-white/10 px-3 py-1 text-xs text-soft">{skill}</span>)}</div><div className="mt-4 flex items-center justify-between text-sm font-semibold"><span>{job.salary}</span><span className="inline-flex items-center gap-1 text-cyanstar">Details <ArrowRight size={15}/></span></div></Link>}
