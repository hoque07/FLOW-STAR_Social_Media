import type {DemoChat} from '../../data/types';import {Avatar} from '../common/Avatar';
export function ChatList({chats,activeChatId,onSelect}:{chats:DemoChat[];activeChatId:string;onSelect:(chatId:string)=>void}){return <div className="space-y-2">{chats.map(chat=><button key={chat.id} onClick={()=>onSelect(chat.id)} className={`focus-ring flex w-full items-center gap-3 rounded-3xl p-3 text-left transition ${activeChatId===chat.id?'bg-cyanstar/18 ring-1 ring-cyanstar/35':'hover:bg-white/8'}`}><Avatar src={chat.avatar} name={chat.title}/><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{chat.title}</p><p className="truncate text-xs text-soft">{chat.last}</p></div>{chat.unread>0&&<span className="grid h-6 min-w-6 place-items-center rounded-full bg-coralstar px-2 text-xs font-bold text-white">{chat.unread}</span>}</button>)}</div>}
