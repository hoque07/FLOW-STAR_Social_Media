import {NavLink} from 'react-router-dom';import {BriefcaseBusiness,Home,MessageCircle,Plus,Clapperboard,UserRound} from 'lucide-react';
const items=[{to:'/home',label:'Home',icon:Home},{to:'/reels',label:'Reels',icon:Clapperboard},{to:'/create-post',label:'Post',icon:Plus},{to:'/chat',label:'Chat',icon:MessageCircle},{to:'/career',label:'Career',icon:BriefcaseBusiness},{to:'/profile/nibirflow',label:'Me',icon:UserRound}];
export function BottomNav(){return <nav className="fixed bottom-3 left-1/2 z-50 flex w-[calc(100%-1.5rem)] max-w-md -translate-x-1/2 justify-between rounded-[1.7rem] border border-white/12 bg-ink/78 p-2 shadow-glass backdrop-blur-2xl light:bg-white/82 lg:hidden">{items.map(({to,label,icon:Icon})=><NavLink key={to} to={to} className={({isActive})=>`flex min-w-0 flex-1 flex-col items-center gap-1 rounded-2xl px-2 py-2 text-[10px] font-bold transition ${isActive?'bg-cyanstar text-slate-950':'text-white/62 light:text-slate-600'}`}><Icon size={18}/><span>{label}</span></NavLink>)}</nav>}
