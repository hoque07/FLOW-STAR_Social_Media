import {Heart,MessageCircle,Play,Send} from 'lucide-react';import {motion} from 'framer-motion';import type {DemoPost} from '../../data/types';import {demoUsers} from '../../data/mock';import {Avatar} from '../common/Avatar';import {compactNumber,isVerified} from '../../utils/format';
export function ReelCard({post}:{post:DemoPost}){const author=demoUsers.find(u=>u.id===post.authorId)!;return <motion.section className="relative h-[78vh] min-h-[560px] snap-center overflow-hidden rounded-[2rem] border border-white/10 bg-black shadow-glass" whileInView={{scale:1}} initial={{scale:.96}} viewport={{amount:.6}}><img src={post.image} alt="Reel frame" className="absolute inset-0 h-full w-full object-cover opacity-80"/><div className="absolute inset-0 bg-gradient-to-t from-black via-black/20 to-transparent"/><button className="absolute left-1/2 top-1/2 grid h-16 w-16 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full bg-white/18 backdrop-blur-xl"><Play fill="currentColor"/></button><div className="absolute bottom-5 left-5 right-20"><div className="mb-3 flex items-center gap-3"><Avatar src={author.avatar} name={author.name} verified={isVerified(author.followers)}/><div><p className="font-bold">@{author.username}</p><p className="text-xs text-white/70">{author.role}</p></div></div><p className="text-sm leading-6 text-white/90">{post.text}</p></div><div className="absolute bottom-8 right-4 flex flex-col gap-3"><button className="grid h-12 w-12 place-items-center rounded-2xl bg-white/14 backdrop-blur-md"><Heart/></button><span className="text-center text-xs font-bold">{compactNumber(post.likes)}</span><button className="grid h-12 w-12 place-items-center rounded-2xl bg-white/14 backdrop-blur-md"><MessageCircle/></button><button className="grid h-12 w-12 place-items-center rounded-2xl bg-white/14 backdrop-blur-md"><Send/></button></div></motion.section>}
