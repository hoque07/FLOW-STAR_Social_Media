import {motion} from 'framer-motion';import {Avatar} from '../common/Avatar';import type {DemoUser} from '../../data/types';import {isVerified} from '../../utils/format';
export function StoryCircle({user}:{user:DemoUser}){return <motion.div whileTap={{scale:.94}} className="w-20 shrink-0 text-center"><motion.div animate={{rotate:[0,8,0]}} transition={{duration:3,repeat:Infinity,delay:Math.random()}} className="mx-auto rounded-3xl bg-gradient-to-tr from-coralstar via-cyanstar to-limestar p-[2px]"><div className="rounded-3xl bg-ink p-1 light:bg-white"><Avatar src={user.avatar} name={user.name} size="lg" verified={isVerified(user.followers)}/></div></motion.div><p className="mt-2 truncate text-xs font-semibold">{user.username}</p></motion.div>}
