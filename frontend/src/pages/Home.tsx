import {Link} from 'react-router-dom';import {Plus} from 'lucide-react';import {Button} from '../components/common/Button';import {PostCard} from '../components/social/PostCard';import {StoryCircle} from '../components/social/StoryCircle';import {demoUsers} from '../data/mock';import {useSocialStore} from '../store/socialStore';import {usePageTitle} from '../hooks/usePageTitle';
export default function Home(){usePageTitle('Home');const posts=useSocialStore(s=>s.posts);return <div className="space-y-5"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-black">Home</h1><p className="text-sm text-soft">Stories, posts, coins, and creator momentum.</p></div><Link to="/create-post"><Button><Plus size={18}/>Create post</Button></Link></div><div className="safe-card flex gap-4 overflow-x-auto p-4">{demoUsers.map(u=><StoryCircle key={u.id} user={u}/>)}</div><div className="space-y-5">{posts.map(p=><PostCard key={p.id} post={p}/>)}</div></div>}
