import {BadgeCheck,Bell,BriefcaseBusiness,Heart,UserPlus,WalletCards} from 'lucide-react';import {demoNotifications} from '../data/mock';import {timeAgo} from '../utils/format';import {usePageTitle} from '../hooks/usePageTitle';
const icons={like:Heart,follow:UserPlus,job:BriefcaseBusiness,coin:WalletCards,verify:BadgeCheck};export default function Notifications(){usePageTitle('Notifications');return <div className="space-y-4"><h1 className="text-3xl font-black">Notifications</h1>{demoNotifications.map(n=>{const Icon=icons[n.type];return <article key={n.id} className={`safe-card flex items-center gap-4 p-4 ${n.read?'':'ring-1 ring-cyanstar/35'}`}><div className="grid h-12 w-12 place-items-center rounded-2xl bg-white/10"><Icon className="text-cyanstar"/></div><div className="flex-1"><p className="font-semibold">{n.text}</p><p className="text-xs text-soft">{timeAgo(n.createdAt)} ago</p></div>{!n.read&&<Bell className="text-coralstar" size={18}/>}</article>})}</div>}
