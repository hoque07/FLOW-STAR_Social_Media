import {useState} from 'react';import {ExternalLink,Heart,MessageCircle,Play,Share2,X} from 'lucide-react';import {Avatar} from '../components/common/Avatar';import {demoUsers} from '../data/mock';import {useSocialStore} from '../store/socialStore';import {usePageTitle} from '../hooks/usePageTitle';import {compactNumber,isVerified} from '../utils/format';
const youtubeId=(url?:string)=>{if(!url)return null;const m=url.match(/(?:youtu\.be\/|youtube\.com\/(?:watch\?v=|embed\/|shorts\/))([\w-]{6,})/);return m?.[1]??null};
const fallbackVideos=['https://www.youtube.com/embed/dQw4w9WgXcQ','https://www.youtube.com/embed/jfKfPfyJRdk','https://www.youtube.com/embed/ysz5S6PUM-U'];
export default function Reels(){usePageTitle('Reels');const posts=useSocialStore(s=>s.posts);const[playing,setPlaying]=useState<string|null>(null);const reels=posts.map((p,i)=>({...p,videoUrl:p.videoUrl||fallbackVideos[i%fallbackVideos.length]}));return <div><h1 className="mb-2 text-3xl font-black">Reels</h1><p className="mb-5 text-sm text-soft">Tap play to open a real playable video frame.</p><div className="h-[calc(100vh-9rem)] snap-y snap-mandatory space-y-5 overflow-y-auto pr-1">{reels.map(post=>{const author=demoUsers.find(u=>u.id===post.authorId)??demoUsers[0];const id=youtubeId(post.videoUrl);const src=id?`https://www.youtube.com/embed/${id}?autoplay=1&rel=0`:post.videoUrl;return <section key={post.id} className="relative h-[78vh] min-h-[560px] snap-center overflow-hidden rounded-[2rem] border border-white/10 bg-black shadow-glass">{playing===post.id?<iframe src={src} title={post.text} className="absolute inset-0 h-full w-full" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen/>:<img src={post.image} alt="Reel frame" className="absolute inset-0 h-full w-full object-cover opacity-80"/>}<div className="pointer-events-none absolute inset-0 bg-gradient-to-t from-black via-black/15 to-transparent"/>{playing===post.id?<button onClick={()=>setPlaying(null)} className="absolute right-4 top-4 grid h-11 w-11 place-items-center rounded-2xl bg-black/60 backdrop-blur"><X/></button>:<button onClick={()=>setPlaying(post.id)} className="absolute left-1/2 top-1/2 grid h-16 w-16 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full bg-white/18 backdrop-blur-xl"><Play fill="currentColor"/></button>}<div className="absolute bottom-5 left-5 right-20"><div className="mb-3 flex items-center gap-3"><Avatar src={author.avatar} name={author.name} verified={isVerified(author.followers)}/><div><p className="font-bold">@{author.username}</p><p className="text-xs text-white/70">Playable reel</p></div></div><p className="text-sm leading-6 text-white/90">{post.text}</p></div><div className="absolute bottom-8 right-4 flex flex-col items-center gap-3"><button className="grid h-12 w-12 place-items-center rounded-2xl bg-white/14 backdrop-blur-md"><Heart/></button><span className="text-center text-xs font-bold">{compactNumber(post.likes)}</span><button className="grid h-12 w-12 place-items-center rounded-2xl bg-white/14 backdrop-blur-md"><MessageCircle/></button><button className="grid h-12 w-12 place-items-center rounded-2xl bg-white/14 backdrop-blur-md"><Share2/></button><a href={post.videoUrl} target="_blank" className="grid h-12 w-12 place-items-center rounded-2xl bg-white/14 backdrop-blur-md"><ExternalLink size={18}/></a></div></section>})}</div></div>}