import {FormEvent,useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {Button} from '../components/common/Button';import {Input} from '../components/common/Input';import {useAuthStore} from '../store/authStore';import {usePageTitle} from '../hooks/usePageTitle';import {AuthFrame} from './Login';
export default function Signup(){usePageTitle('Signup');const[name,setName]=useState('Flow Starter');const[username,setUsername]=useState('flowstarter');const signup=useAuthStore(s=>s.signup);const navigate=useNavigate();const submit=(e:FormEvent)=>{e.preventDefault();signup(name,username);navigate('/home')};return <AuthFrame title="Create your star" action="Signup" submit={submit}><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Full name"/><Input value={username} onChange={e=>setUsername(e.target.value)} placeholder="username"/><Input type="password" placeholder="password" defaultValue="flowstar"/><Button className="w-full">Create account</Button><p className="text-center text-sm text-soft">Already joined? <Link className="text-cyanstar" to="/login">Login</Link></p></AuthFrame>}
