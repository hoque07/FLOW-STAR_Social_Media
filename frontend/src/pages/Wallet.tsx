import {WalletCards} from 'lucide-react';import {useCoinStore} from '../store/coinStore';import {usePageTitle} from '../hooks/usePageTitle';
export default function Wallet(){usePageTitle('Wallet');const{coins,transactions}=useCoinStore();return <div className="space-y-5"><section className="safe-card overflow-hidden p-6"><div className="flex items-center gap-4"><div className="grid h-16 w-16 place-items-center rounded-3xl bg-goldstar text-slate-950"><WalletCards size={30}/></div><div><p className="text-sm text-soft">FLOW coins</p><h1 className="text-5xl font-black">{coins}</h1></div></div><div className="mt-6 grid grid-cols-3 gap-3 text-center"><Rule label="Like" value="+1"/><Rule label="Follow" value="+10"/><Rule label="Post" value="+5"/></div></section><section className="safe-card p-5"><h2 className="mb-4 text-xl font-black">Transaction history</h2><div className="space-y-2">{transactions.map(t=><div key={t.id} className="flex items-center justify-between rounded-2xl bg-white/8 p-3"><div><p className="font-semibold">{t.label}</p><p className="text-xs text-soft">{new Date(t.createdAt).toLocaleString()}</p></div><b className={t.amount>0?'text-limestar':'text-coralstar'}>{t.amount>0?'+':''}{t.amount}</b></div>)}</div></section></div>}
function Rule({label,value}:{label:string;value:string}){return <div className="rounded-3xl bg-white/10 p-4"><p className="text-2xl font-black text-goldstar">{value}</p><p className="text-xs text-soft">{label}</p></div>}
